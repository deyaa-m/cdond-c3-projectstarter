## Copy backend files from circleci docker container to server
---
- name: create app dir
  file:
    path: ~/udapeople
    state: directory

- name: copy files
  copy:
    src: ~/project/backend/.dist/
    dest: ~/udapeople
    owner: ubuntu
    group: ubuntu
    force: no